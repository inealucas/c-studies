# Daily Dev - 2026-05-22

## What I studied today
- Arithmetic operators in C (+, -, *, /, %)
- Integer division (removes decimal part)
- Operator precedence (order of execution in expressions)
- if / else condition with modulus (%)
- Compound assignment operators (+=, -=, *=, /=)

## Key concepts
- In C, division between integers returns an integer result (decimal is truncated).
- The operator `%` returns the remainder of a division.
- Parentheses `()` change the order of evaluation in expressions.
- Expressions are evaluated from left to right respecting operator precedence.

## Practice done
- Basic arithmetic operations between integers
- Difference between `x / y` and `x % y`
- Parity check using `if (x % 2 == 0)`
- Compound assignment operations

## Notes
- Need to reinforce operator precedence rules
- Need more exercises mixing multiple operators in one expression